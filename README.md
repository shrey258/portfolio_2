# Portfolio

A clean, minimal portfolio website built with React and TypeScript.

## Setup

Install dependencies:

```bash
npm install
```

Start the development server:

```bash
npm run dev
```

Build for production:

```bash
npm run build
```
